json.extract! track, :id, :player_id, :x, :z, :xMax, :zMax, :xMin, :zMin, :xP, :zP, :xN, :zN, :zMaxN, :zMaxN, :xMinN, :zMinN, :xPN, :zPN, :raided, :notes, :created_at, :updated_at
json.url track_url(track, format: :json)
